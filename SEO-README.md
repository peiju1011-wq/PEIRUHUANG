# PEIRU HUANG SEO / AI Search update

- Corrected the case-sensitive GitHub Pages canonical URL to `/PEIRUHUANG/`.
- Added richer bilingual artist metadata and social sharing metadata.
- Added Schema.org `Person`, `WebSite`, and `VisualArtwork` structured data for 12 works.
- Added `robots.txt` and `sitemap.xml`.
- Added a no-JavaScript artwork fallback for crawlers.
- Public artwork prices remain hidden.

Note: the site currently uses HashRouter (`#/work/...`). Hash routes are not separate server URLs, so the sitemap intentionally lists the canonical site URL only. A future routing migration can give every artwork its own indexable URL.
